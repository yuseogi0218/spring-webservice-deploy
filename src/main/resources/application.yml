spring:
  # H2-DB setting
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb # h2-DB url
    driver-class-name: org.h2.Driver
    username: sa
    password:


#HandleBars
handlebars:
  enabled: true
  prefix:
    classpath: /templates/
  suffix: .hbs
  cache: false